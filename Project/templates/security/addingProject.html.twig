{% extends 'basedash.html.twig' %}

{% block title %}
    Admin | Rajouter un projet
{% endblock %}

{% block body %}
        <div class="container-fluid">
            {{ form_start(form, {attr: {class: 'form-group'} }) }}
                {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>

{% endblock %}
