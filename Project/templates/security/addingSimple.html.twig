{% extends 'basedash.html.twig' %}

{% block title %}
    Admin | Rajouter {{ slug }}
{% endblock %}

{% block body %}
<div class="container-fluid">
  <h1>Rajouter {{ slug }}</h1>
    {{ form_start(form, {attr: {class: 'form-group'} }) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}
